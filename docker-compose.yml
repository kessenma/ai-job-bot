services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - app-data:/app/data
    env_file:
      - ./apps/web/.env
    restart: unless-stopped

  # Future: on-prem LLM for cover letter generation
  # llm:
  #   image: ollama/ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - llm-models:/root/.ollama

volumes:
  app-data:
  # llm-models:
